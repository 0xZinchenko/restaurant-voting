spring:
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 20
        jdbc.batch_size: 20
  datasource:
    # ImMemory
    url: jdbc:h2:mem:voting
    #    tcp: jdbc:h2:tcp://localhost:9092/mem:voting
    username: yz
    password:
  h2:
    console:
      enabled: true

  data:
    rest:
      basePath: /api
      return-body-on-create: true

#  security:
#    user:
#      name: user
#      password: password
#      roles: USER

logging:
      level:
        root: WARN
        com.zim4ik.restaurant-voting: DEBUG
#        org.springframework.security.web.FilterChainProxy: DEBUG

    # Jackson Serialization Issue Resolver
    #  jackson:
    #    visibility.field: any

#  jackson:
#    visibility:
#      field: any
#      getter: none
#      setter: none
#      is-getter: none